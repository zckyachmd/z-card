# ============================================
# Environment Variables Example
# ============================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control

# ============================================
# Cloudflare Turnstile CAPTCHA
# ============================================
# Get your keys from: https://dash.cloudflare.com/?to=/:account/turnstile
# Site Key is public and can be exposed to the browser
# Secret Key must be kept private and only used server-side

# Public site key (exposed to browser)
NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY=your_site_key_here

# Secret key (server-side only, never expose to browser)
CLOUDFLARE_TURNSTILE_SECRET_KEY=your_secret_key_here

# ============================================
# SMTP Email Configuration
# ============================================
# Required for contact form email functionality
# If not configured, contact form will fail to send emails

# SMTP server hostname
SMTP_HOST=smtp.example.com

# SMTP server port (587 for TLS, 465 for SSL, 25 for unencrypted)
SMTP_PORT=587

# SMTP authentication username
SMTP_USER=your_smtp_username

# SMTP authentication password
SMTP_PASS=your_smtp_password

# Email address to send from (must match SMTP_USER domain)
SMTP_FROM_EMAIL=noreply@example.com

# Email address to receive contact form submissions
SMTP_TO_EMAIL=contact@example.com

# Use secure connection (true for port 465, false for other ports)
SMTP_SECURE=false

# Reject unauthorized SSL certificates (set to false for self-signed certs)
SMTP_REJECT_UNAUTHORIZED=true

# ============================================
# Rate Limiting
# ============================================
# Optional: Configure rate limiting for API endpoints
# Defaults: 5 requests per 60 seconds

# Maximum number of requests allowed per window
RATE_LIMIT_MAX_REQUESTS=5

# Time window in milliseconds (60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# ============================================
# Robot Validation
# ============================================
# Optional: Configure minimum submission time for bot detection
# Default: 2000ms (2 seconds)

# Minimum time in milliseconds between form load and submission
ROBOT_MIN_SUBMISSION_TIME=2000

# ============================================
# Node Environment
# ============================================
# Automatically set by Next.js, but can be overridden
# Options: development, production, test
# NODE_ENV=development
