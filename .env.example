# Environment Variables Template for z-card
# Copy this file to .env and fill in your actual values
# Never commit .env file to version control

# ============================================
# Current Status: No environment variables required
# ============================================
# The project currently works without any environment configuration
# as it is a frontend-only application.

# ============================================
# SMTP Email Service Configuration
# ============================================
# Required for contact form functionality (Epic 5)

# SMTP Server Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
# SMTP_SECURE=true for port 465 (SSL), false for port 587 (TLS)

# SMTP Authentication
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Email Addresses
SMTP_FROM_EMAIL=noreply@example.com
SMTP_TO_EMAIL=contact@example.com

# SMTP TLS Options (Optional)
# SMTP_REJECT_UNAUTHORIZED=true

# ============================================
# Rate Limiting (Optional)
# ============================================
# Configure rate limiting untuk prevent spam

# Maximum requests per window (default: 5)
RATE_LIMIT_MAX_REQUESTS=5

# Rate limit window in milliseconds (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000

# ============================================
# Robot Validation (Optional)
# ============================================
# Configure robot/bot detection

# Minimum submission time in milliseconds (default: 2000 = 2 seconds)
# Submissions faster than this are considered bots
ROBOT_MIN_SUBMISSION_TIME=2000

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and add your actual values
# 3. Never commit .env to version control
